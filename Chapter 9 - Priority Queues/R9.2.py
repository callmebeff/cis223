"""
R9.2

Suppose you label each position p of a binary tree T with a key equal to
its preorder rank. Under what circumstances is T a heap?
"""

"""
T would be a heap in this sense, since all keys will be equal, as long as the root is less than all other keys and at its depth all children are in the leftmost position. 

"""